## LiteLLM proxy config (OpenAI-compatible /v1/* API)
##
## This lets ai-service talk to multiple model providers through a single OpenAI-compatible endpoint.
## ai-service must be pointed at this gateway:
##   OPENAI_BASE_URL=http://litellm:4000/v1
## and OPENAI_MODEL should be one of the model_name values below.
##
## Provider API keys are read from environment variables (via `.env`).
## - OpenAI:     OPENAI_API_KEY
## - Anthropic:  ANTHROPIC_API_KEY
## - Gemini:     GEMINI_API_KEY
##
## Note: If you enable LiteLLM auth with LITELLM_MASTER_KEY, ai-service's OPENAI_API_KEY
## must match that master key (and you'll likely want to move provider keys to different env vars).

model_list:
  # OpenAI
  - model_name: gpt-4o-mini
    litellm_params:
      model: openai/gpt-4o-mini
      api_key: os.environ/OPENAI_API_KEY

  # Claude (Anthropic) - disabled by default
  # - model_name: claude-3-5-sonnet
  #   litellm_params:
  #     model: anthropic/claude-3-5-sonnet-20241022
  #     api_key: os.environ/ANTHROPIC_API_KEY

  # Gemini - disabled by default
  # - model_name: gemini-2.0-flash
  #   litellm_params:
  #     model: gemini/gemini-2.0-flash
  #     api_key: os.environ/GEMINI_API_KEY

  # OpenAI (placeholder)
  # - model_name: openai-placeholder
  #   litellm_params:
  #     model: openai/<MODEL_ID>
  #     api_key: os.environ/OPENAI_API_KEY

  # Anthropic (placeholder)
  # - model_name: claude-placeholder
  #   litellm_params:
  #     model: anthropic/<MODEL_ID>
  #     api_key: os.environ/ANTHROPIC_API_KEY

  # Azure OpenAI (placeholder)
  # - model_name: azure-openai-placeholder
  #   litellm_params:
  #     model: azure/<DEPLOYMENT_NAME>
  #     api_key: os.environ/AZURE_OPENAI_API_KEY
  #     api_base: os.environ/AZURE_OPENAI_ENDPOINT
  #     api_version: os.environ/AZURE_OPENAI_API_VERSION

  # Ollama (placeholder)
  # - model_name: ollama-placeholder
  #   litellm_params:
  #     model: ollama/<MODEL_ID>
  #     api_base: http://ollama:11434

  # Gemini Vertex AI (placeholder)
  # - model_name: gemini-vertex-placeholder
  #   litellm_params:
  #     model: vertex_ai/<MODEL_ID>
  #     project: os.environ/GCP_PROJECT_ID
  #     location: os.environ/GCP_LOCATION

  # Anthropic Vertex AI (placeholder)
  # - model_name: anthropic-vertex-placeholder
  #   litellm_params:
  #     model: vertex_ai/<MODEL_ID>
  #     project: os.environ/GCP_PROJECT_ID
  #     location: os.environ/GCP_LOCATION

  # Bedrock (placeholder) - disabled by default
  # Replace <MODEL_ID> with a Bedrock model ID (e.g., anthropic.claude-3-5-sonnet-20241022-v2)
  # AWS credentials/region are picked up from standard AWS env vars (or IRSA in K8s).
  # - model_name: bedrock-placeholder
  #   litellm_params:
  #     model: bedrock/<MODEL_ID>

general_settings:
  # Keep the OpenAI-compatible endpoints enabled.
  # (default is true; explicit here for clarity)
  openai_compatible_endpoints: true
